name: Phylogenetic-trees experiment environment
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.5
      uses: actions/setup-python@v1
    - name: Build Container
      run: |
        make build
